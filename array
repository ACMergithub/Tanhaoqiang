#include <stdio.h>
int fun(int arr[]) 
{
    int i,sum=0;
    for(i=0;i<10;i++) sum+=arr[i];
    return(sum/10);
}

int main()
{
    int i,a[10],b[10]={0};
    for(i=0;i<10;i++) scanf("%d",&a[i]);
    for(i=0;i<10;i++) 
    {
        if(a[i]>fun(a))
            b[i]=a[i];
    }
    for(i=0;i<10;i++) 
    {
        if(b[i]!=0) printf("%d ",b[i]);
    }
    return 0;
}
